# Optional live integration resources. Copy to .env.test at the repository root.
# Use a separate, disposable PostgreSQL database named complyos_test.
TEST_DATABASE_URL=
# Use a separate Redis instance or database; must differ from the application's Redis.
# A unique per-run queue prefix isolates test jobs. No FLUSHALL or FLUSHDB is used.
TEST_REDIS_URL=
