# Copy to apps/worker/.env. Use the same database and Redis as the API.
# These are external services; the application does not start them.
NODE_ENV=development
QUEUE_PREFIX=complyos

# postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require
DATABASE_URL=

# rediss://default:PASSWORD@HOST:6379/0
REDIS_URL=
